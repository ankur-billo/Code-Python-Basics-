import shutil
import os

# shutil.copy("task.txt","Practice Set  IO/") #copy the file to the location

# shutil.move("task.txt","Practice Set  IO/") #copy the file to the location

os.remove("task.txt")